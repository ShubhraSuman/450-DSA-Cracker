-------------------------------------------------------STRING---------------------------------------------------------------------------------------------------
1) char a[1000] = "apple";
to calculate length strlen(a) -> 5
--------------------------------------------------------------------------------------------------------------------------------------------------------
2) strcpy(destination, source)
char b[1000];
strcpy(b,a);
cout<<b; //apple
--------------------------------------------------------------------------------------------------------------------------------------------------------
3) strcmp():
- check two strings are equal or not
cout<< strcmp(a,b); //0
--------------------------------------------------------------------------------------------------------------------------------------------------------
4) strcat:
char web[] = "www.";
char domain[] = "codingminutes.com";
cout<<strcat(web,domain);// wwww.codingminutes.com
--------------------------------------------------------------------------------------------------------------------------------------------------------
5) string s("Hello world"); //Calling a constructor to initialise object.
--------------------------------------------------------------------------------------------------------------------------------------------------------
6) To take string input with multiple lines
- getline(cin,s);
--------------------------------------------------------------------------------------------------------------------------------------------------------
7) To take input of string till a '.' is encountered
- getline(cin,s,'.')
--------------------------------------------------------------------------------------------------------------------------------------------------------
8) To initialise vector of array
- std::vector<char> chars{'a','a','b','b','c','c','c'};
--------------------------------------------------------------------------------------------------------------------------------------------------------
9) To copy string into vector of characters
- std::copy(res.begin(), res.end(), std::back_inserter(chars))
- here back_inserter will copy characters of string at the end of vector.
--------------------------------------------------------------------------------------------------------------------------------------------------------
